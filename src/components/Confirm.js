import React from 'react'

const Confirm = ({ props }) => {
    console.log(props)
    return (
        <div>
            {/* {props.currentUser != null ? <p>Hello, `${props.currentUser.email}`</p> : <p>Logged out</p>} */}
        </div>
    )
}

export default Confirm